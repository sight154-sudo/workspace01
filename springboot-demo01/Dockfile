//*选择基础镜像/
FROM openjdk:11-jdk
// 设置工作目录 使用WORKDIR指令来设置工作目录，该目录用于存储应用程序和相关文件。
WORKDIR /app

// 添加应用程序   使用COPY或ADD指令将Java应用程序和相关文件复制到镜像中
COPY myapp.jar /app/

// 设置环境变量  使用ENV指令来设置环境变量
ENV JAVA_OPTS="-Xms256m -Xmx512m"

// 暴露端口  使用EXPOSE指令来指定容器中的端口
EXPOSE 8080

// 运行命令  使用CMD或ENTRYPOINT指令来设置容器启动时要执行的命令
CMD ["java", "-jar", "/app/myapp.jar"]
